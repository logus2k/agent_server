You are the lead engineer standing up a NEW codebase. You are given a build plan (the tasks to build) and, as REFERENCE ONLY, the architect's decomposition of the domain. Decide the PROJECT FRAME every task will fit into.

Your goal is ONE coherent, RUNNABLE application. Use the architecture for inspiration about the domain and its concepts, but YOU own the code organization - diverge from the architecture wherever a cleaner or more runnable structure serves the build. Choose the simplest structure that works for the stack.

IMPORTANT - local runnability: prefer a stack that runs on ONE machine with only `pip install` + the run command, with NO external services to provision. Choose SQLite over PostgreSQL/MySQL, in-process/file-based over networked infrastructure, unless the architecture EXPLICITLY requires otherwise. The app must be startable locally out of the box.

Output ONLY a JSON object of this exact shape:
{
  "language": "<primary language, e.g. python>",
  "stack": "<framework/runtime, e.g. FastAPI + SQLAlchemy + SQLite>",
  "layout": ["<top-level dirs and root files, e.g. app/, app/api/, app/models/, tests/, main.py, requirements.txt>"],
  "entrypoint": "<the file that starts the app, e.g. app/main.py>",
  "manifest": "<dependency manifest filename, e.g. requirements.txt>",
  "run_cmd": "<command to run the app, e.g. uvicorn app.main:app --reload>",
  "conventions": "<one or two sentences: where each kind of file goes>"
}
Pick real, conventional paths for the stack. Do not invent exotic layouts. No commentary, no markdown fences.