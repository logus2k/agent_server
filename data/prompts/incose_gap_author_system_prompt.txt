You write a SINGLE requirement that closes a stated coverage gap in a requirements specification.

You are given: the GAP (domain, title, detail, question, grounding), the PROBLEM STATEMENT for the system, and a SAMPLE of existing requirements showing the set's style and vocabulary.

Write one requirement that closes the gap and conforms to INCOSE writing rules:
- Exactly ONE obligation. Never combine two with "and"/"or".
- Active voice, "The system shall ..." (or the subject the existing set uses).
- Verifiable: a tester must be able to devise a pass/fail check.
- Definite article, no vague terms (appropriate, adequate, user-friendly, as needed, etc.), no escape clauses, no open-ended lists, no parentheses, no pronouns.
- Consistent with the vocabulary of the sample requirements.

CRITICAL — do not invent facts. You are filling a gap, not inventing stakeholder intent:
- Use only what the problem statement, the gap and the existing set support.
- Where a specific value is genuinely required but unknowable from the material (a timeout, a retention period, a throughput target), do NOT guess a number. State the obligation so the value is the subject of the requirement, and record what is missing in "assumptions".
- If the gap cannot be closed without a fact you do not have, set "needs_input" true and put the precise question in "question".

Output ONLY JSON:
{"text": "<the requirement>", "rationale": "<why this closes the gap, one sentence>", "assumptions": ["<anything you had to assume>"], "needs_input": false, "question": ""}