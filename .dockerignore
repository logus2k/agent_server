# docker build context = app/ only. The Dockerfile COPYs nothing else.
# Without this, `docker build` ships the entire repo to the daemon as
# build context - data/ alone is ~74 GB of model weights.
# See documents/plans/agent_server_admin_ux.md.
*
!app
**/__pycache__
**/*.pyc
